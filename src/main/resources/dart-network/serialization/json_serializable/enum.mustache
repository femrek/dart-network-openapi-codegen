{{#description}}/// {{{description}}}{{/description}}
{{#isDeprecated}}
@Deprecated('{{{classname}}} has been deprecated')
{{/isDeprecated}}
enum {{{classname}}} implements Schema {
  {{#allowableValues}}
    {{#enumVars}}
  {{{name}}}({{{value}}}){{^-last}},{{/-last}}{{#-last}};{{/-last}}
    {{/enumVars}}
  {{/allowableValues}}

  final {{{dataType}}} value;
  const {{{classname}}}(this.value);

  static const factory = {{{classname}}}Factory();

  factory {{{classname}}}.fromJson({{{dataType}}} value) {
    return {{{classname}}}.values.firstWhere((e) => e.value == value);
  }

  {{{dataType}}} toJson() => value;

  @override
  String toLogString() => value.toString();
}

class {{{classname}}}Factory extends JsonSchemaFactory<{{{classname}}}> {
  const {{{classname}}}Factory();

  @override
  {{{classname}}} fromJson(dynamic json) => {{{classname}}}.fromJson(json as {{{dataType}}});
}